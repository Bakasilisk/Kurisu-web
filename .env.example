DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
# What to register under the Discord application's OAuth2 -> Redirects list.
# The running code always derives the actual redirect URI from PUBLIC_BASE_URL
# below (+ /auth/callback) — keep these in agreement.
DISCORD_REDIRECT_URI=https://kurisu.magicsociety.moe/auth/callback
PUBLIC_BASE_URL=https://kurisu.magicsociety.moe
SESSION_SECRET=
BOT_API_BASE=http://127.0.0.1:8080
WEBAPI_KEY=
WEB_BIND=127.0.0.1:8081
